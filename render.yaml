services:
  - type: web
    name: forge-ai
    runtime: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 22.13.0
      - key: ANTHROPIC_API_KEY
        sync: false
